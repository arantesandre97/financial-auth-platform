-- V1__placeholder.sql
-- Migration placeholder — mantém o Flyway satisfeito enquanto as tabelas
-- ainda não foram criadas. Substituir pelo schema real na S1 (auth.users, etc).
-- Este arquivo NÃO cria nenhum objeto — é intencionalmente vazio de DDL.

SELECT 1; -- no-op